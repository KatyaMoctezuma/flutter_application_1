import 'package:flutter/material.dart';

class Note {
  final String title;
  final String content;
  final Color color;
  final IconData icon;
  Note({
    required this.title,
    required this.content,
    required this.color,
    required this.icon,
  });
}
